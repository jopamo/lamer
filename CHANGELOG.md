# Changelog

## lamer

- rename Meson project identifier to `lamer`
- keep CLI and library compatibility for drop-in replacement use
- add CLI identity smoke coverage
- make `-V0`/`--preset extreme` keep a conservative default VBR floor for active music unless the user explicitly sets `-b`
