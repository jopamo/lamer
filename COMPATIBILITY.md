# Compatibility

lamer is meant to remain a drop-in replacement for LAME.

Stable external surfaces:
- `lame` executable name
- `libmp3lame.so.0` shared library identity
- `lame.h` public header
- `lame_*` public API naming
- LAME-compatible bitstream tag identity
